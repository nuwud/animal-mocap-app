import { AppRegistry } from 'react-native';
import App from './App';

// Register the main component with a specific name to match MainActivity.java
AppRegistry.registerComponent('AnimalMoCapApp', () => App);